{% extends "::layout.html.twig" %}

{% block title %}AppBundle:Accueil:accueil{% endblock %}

{% block titre %}Accueil{% endblock %}
	
{% block contenu %}
    {% if is_granted('ROLE_ADMIN') %}
        {{render(controller('AppBundle:Accueil:accueilAdmin'))}}
    {% else %}
         {{render(controller('AppBundle:Accueil:accueilUser'))}}
    {% endif %}
{% endblock %}
